import { Application } from 'express';
import { isTokenValid } from '@globals/middlewares/isTokenValid';

const BASE_PATH = '/api/v1';

export default (app: Application) => {
  const routes = () => {};
  routes();
};
